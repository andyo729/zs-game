<ul>
  {{#each model.users as |user|}}
    <li>
      {{user.name}}
      {{#each model.pointTypes as |pointType|}}
        <button {{action "addPoint" user pointType}}>{{pointType.name}}</button>
      {{/each}}
    </li>
  {{/each}}
</ul>
{{outlet}}
